version: 2

models:
  - name: stg_coins
    description: "Date brute despre monede crypto"
    columns:
      - name: coin_id
        description: "ID unic al monedei"
        tests:
          - not_null
      - name: price_usd
        description: "Pretul in USD"
        tests:
          - not_null